{
  "name": "tif-ing-soft-3",
  "version": "0.1.0",
  "description": "Analizador de malware potenciado por IA",
  "private": true,
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node src/server.js",
    "build": "node -c src/server.js && node -c public/app.js",
    "test": "jest",
    "lint": "eslint ."
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "eslint": "^9.5.0",
    "jest": "^30.3.0"
  },
  "overrides": {
    "glob": "^11.0.3",
    "test-exclude": "^8.0.0"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
